const PasswordLabelValue = ({ value }) => {
  return (
    <div className=" ml-5 mt-3">
      <h2 className="font-bold">Password</h2>
      <input onChange={() => {}} type="password" value={value} />
    </div>
  );
};

export default PasswordLabelValue;
